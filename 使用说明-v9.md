# 闪蝶售价计算器 v9 使用说明

## 从外币售价反推地接社成本

1. 先确认上方的全团小费、总人数、利润百分比和汇率正确。
2. 双击下方任一个 `USD 售价`、`SGD 售价` 或 `MYR 售价`。
3. 用键盘输入已经报给客人的整数售价。
4. 按 `Enter`，或点击输入框以外的位置。
5. 程序会把反推结果填回最上方的“地接社每人 RMB 成本”，并立即重新计算三种外币售价。

反推时会先还原加利润前的每人总成本，再减去“全团小费 ÷ 总人数”，所以填回的是地接社每人成本。由于客人售价通常已经向上取整，反推成本可能与最初文件有轻微差异。

按 `Esc` 可取消本次售价编辑。输入必须是大于或等于 0 的整数。

## 加减乘除

1. 展开“加减乘除”。
2. 点击显示框，用键盘输入第一个整数。
3. 点击 `＋`、`－`、`×` 或 `÷`。
4. 用键盘输入第二个整数，按 `Enter` 得到结果。

`Backspace` 删除数字，`Esc` 清除并恢复为 0。除法结果四舍五入为整数，除以 0 会显示错误。

## 蝴蝶

新版继续保持窗口强制置顶、鼠标活动时飞回左下角、10 秒无操作自由飞翔、双击展开计算面板、粒子效果和单实例运行。

